SELECT 'BATTERIES' AS Table_Name,
       COUNT(DISTINCT Battery_ID) AS Battery_Count
FROM BATTERIES

UNION ALL

SELECT 'TEST_CYCLES',
       COUNT(DISTINCT Battery_ID)
FROM TEST_CYCLES

UNION ALL

SELECT 'CYCLE_FEATURES',
       COUNT(DISTINCT Battery_ID)
FROM CYCLE_FEATURES;